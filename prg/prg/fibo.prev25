# Copyright (c) 2025 Prev25 Wizards
fun putint(x: int): void
fun puts(str: ^char): void

fun fib(n: int): int =
    if n <= 1 then
        return n
    end,

    return fib(n - 1) + fib(n - 2)

fun main(): int =
    let var i: int in
        i = 0,
        while i <= 15 do
            #puts("fib("),
            #putint(i),
            #puts(") = "),
            putint(fib(i)),
            puts(" "),
            i = i + 1
        end
    end,
    #putint(fib(10)),
    return 0